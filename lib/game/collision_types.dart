/// Collision types for type-safe collision detection in Ball Bounce Blitz
enum CollisionType {
  ball,
  enemy,
  powerUp,
  barrier,
  paddle,
}

/// Collision categories for grouping collision types
enum CollisionCategory {
  none,
  ball,
  enemy,
  powerUp,
  barrier,
  paddle,
}